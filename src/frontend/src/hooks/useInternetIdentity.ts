// Re-export from the core-infrastructure package for backward compatibility
export { useInternetIdentity } from "@caffeineai/core-infrastructure";
export type {
  InternetIdentityContext,
  Status,
} from "@caffeineai/core-infrastructure";
